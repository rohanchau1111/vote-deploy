apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
    tier: back
spec:
  type: ClusterIP
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: result
