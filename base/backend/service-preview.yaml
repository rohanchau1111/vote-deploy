apiVersion: v1
kind: Service
metadata:
  name: result-preview
  labels:
    app: result
    tier: back
spec:
  type: NodePort
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30101
  selector:
    app: result
